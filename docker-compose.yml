version: '3.8'

services:
  web:
    build: .
    container_name: green_portfolio_web
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - NODE_ENV=production
    restart: unless-stopped
